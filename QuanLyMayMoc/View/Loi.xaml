<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="QuanLyMayMoc.Loi"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QuanLyMayMoc"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <local:BooleanToBrushConverter x:Key="BooleanToBrushConverter"/>
        <local:BooleanToBorderBrushConverter x:Key="BooleanToBorderBrushConverter"/>
    </Page.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <Grid Grid.Row="0">
            <TextBlock Text="CHI TIẾT GIÁ LÕI AOSMITH ĐÀ NẴNG"
                   HorizontalAlignment="Center"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,10"
                   Foreground="Black"/>
        </Grid>

        <!-- Tiêu đề cột -->
        <Grid Grid.Row="1" HorizontalAlignment="Center" Background="Gray">
            <Grid Padding="5" RowSpacing="5" ColumnSpacing="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="600"/>
                    <ColumnDefinition Width="350"/>
                </Grid.ColumnDefinitions>
                <Border BorderBrush="Black" BorderThickness="1" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock Text="MÃ SẢN PHẨM" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5"/>
                </Border>
                <Border BorderBrush="Black" BorderThickness="1" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock Text="TÊN SẢN PHẨM" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5"/>
                </Border>
                <Border BorderBrush="Black" BorderThickness="1" Grid.Row="0" Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock Text="GIÁ SẢN PHẨM" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5"/>
                </Border>
            </Grid>
        </Grid>

        <!-- Nội dung cuộn -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" HorizontalAlignment="Center">
            <StackPanel>

                <ListView ItemsSource="{x:Bind ViewModel.ListLoi, Mode=TwoWay}" 
                         x:Name="LoiListView" 
                         SelectionMode="Single" 
                         HorizontalAlignment="Center" 
                         IsItemClickEnabled="True">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:Loisp">
                            <Grid Padding="3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200" />
                                    <ColumnDefinition Width="600" />
                                    <ColumnDefinition Width="350" />
                                </Grid.ColumnDefinitions>

                                <!-- Các Border cho dữ liệu -->
                                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="{x:Bind MaSanPham}" HorizontalAlignment="Center" Foreground="Black" Margin="5" />
                                </Border>
                                <Border BorderBrush="Gray" BorderThickness="2" CornerRadius="5" Grid.Column="1" Margin="0,0,5,0">
                                    <TextBlock Text="{x:Bind TenSanPham}" HorizontalAlignment="Center" Foreground="Black" Margin="5" />
                                </Border>
                                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Grid.Column="2" Margin="0,0,5,0">
                                    <TextBlock Text="{x:Bind GiaBan}" HorizontalAlignment="Center" Foreground="Black" Margin="5" />
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Nội dung Row 3 -->
                <Grid Padding="10" RowSpacing="5" ColumnSpacing="5" Background="White" x:Name="DataEntryGrid">
                    <Grid x:Name="InputGrid" ColumnSpacing="5" RowSpacing="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="600"/>
                            <ColumnDefinition Width="350"/>
                        </Grid.ColumnDefinitions>
                    </Grid>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <!-- Các nút thao tác -->
        <Grid Grid.Row="3">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Thêm Lõi" Width="150" Margin="20" Style="{StaticResource AccentButtonStyle}" Foreground="Black" BorderBrush="Black" BorderThickness="1" Click="OnAddRowDataClick"/>
                <Button Content="Lưu dữ liệu" Width="150" Margin="20" Style="{StaticResource AccentButtonStyle}" Foreground="Black" BorderBrush="Black" BorderThickness="1" Click="OnSaveDataClick"/>
                <Button Content="Sửa dòng dữ liệu" Width="150" Margin="20" Style="{StaticResource AccentButtonStyle}" Foreground="Black" BorderBrush="Black" BorderThickness="1" Click="OnUpdateRowDataClick"/>
                <Button Content="Xóa Lõi" Width="150" Margin="10" Style="{StaticResource AccentButtonStyle}" Foreground="Black" BorderBrush="Black" BorderThickness="1" Click="OnDeleteRowDataClick"/>
                <Button Content="Xóa tất cả" Width="150" Margin="10" Style="{StaticResource AccentButtonStyle}" Foreground="Black" BorderBrush="Black" BorderThickness="1" Click="OnDeleteAllRowDataClick"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>