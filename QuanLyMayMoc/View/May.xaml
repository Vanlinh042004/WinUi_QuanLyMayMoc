<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="QuanLyMayMoc.May"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QuanLyMayMoc"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <local:BooleanToBrushConverter x:Key="BooleanToBrushConverter"/>
        <local:BooleanToBorderBrushConverter x:Key="BooleanToBorderBrushConverter"/>
    </Page.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <Grid Grid.Row="0">
            <TextBlock Text="CHI TIẾT GIÁ LINH KIỆN AOSMITH ĐÀ NẴNG"
                    HorizontalAlignment="Center"
                    FontSize="24"
                    FontWeight="Bold"
                    Margin="0,0,0,10"
                    Foreground="Black"/>
        </Grid>

        <!-- Tiêu đề cột -->
        <Grid Grid.Row="1" HorizontalAlignment="Center" Background="Gray">
            <Grid Padding="5" RowSpacing="5" ColumnSpacing="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="100" />
                </Grid.ColumnDefinitions>
                <Border BorderBrush="Black" BorderThickness="1" Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock Text="THÁNG" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5"/>
                </Border>
                <Border BorderBrush="Black" BorderThickness="1" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock Text="MÃ SẢN PHẨM" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5"/>
                </Border>
                <Border BorderBrush="Black" BorderThickness="1" Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock Text="TÊN SẢN PHẨM" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5"/>
                </Border>
                <Border BorderBrush="Black" BorderThickness="1" Grid.Column="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock Text="SỐ LƯỢNG" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5"/>
                </Border>
                <Border BorderBrush="Black" BorderThickness="1" Grid.Column="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock Text="GIÁ BÁN" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5"/>
                </Border>
                <Border BorderBrush="Black" BorderThickness="1" Grid.Column="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock Text="TỔNG TIỀN" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5"/>
                </Border>
            </Grid>
        </Grid>

        <!-- Nội dung cuộn -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" HorizontalAlignment="Center">
            <StackPanel>

                <ListView ItemsSource="{x:Bind ViewModel.MonthlyProductSummarys, Mode=OneWay}" 
                       x:Name="MonthlyProductSummarysView" 
                       SelectionMode="Single" 
                       HorizontalAlignment="Center" 
                       IsItemClickEnabled="True">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:MonthlyProductSummary">
                            <Grid Padding="3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="150" />
                                    <ColumnDefinition Width="200" />
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="100" />
                                </Grid.ColumnDefinitions>

                                <!-- Các Border cho dữ liệu -->
                                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="{x:Bind Month}" HorizontalAlignment="Center" Foreground="Red" Margin="5" />
                                </Border>
                                <Border BorderBrush="Gray" BorderThickness="2" CornerRadius="5" Grid.Column="1" Margin="0,0,5,0">
                                    <TextBlock Text="{x:Bind ProductCode}" HorizontalAlignment="Center" Foreground="Red" Margin="5" />
                                </Border>
                                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Grid.Column="2" Margin="0,0,5,0">
                                    <TextBlock Text="{x:Bind ProductName}" HorizontalAlignment="Center" Foreground="Red" Margin="5" />
                                </Border>
                                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Grid.Column="3" Margin="0,0,5,0">
                                    <TextBlock Text="{x:Bind Quantity}" HorizontalAlignment="Center" Foreground="Red" Margin="5" />
                                </Border>
                                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Grid.Column="4" Margin="0,0,5,0">
                                    <TextBlock Text="{x:Bind Price}" HorizontalAlignment="Center" Foreground="Red" Margin="5" />
                                </Border>
                                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Grid.Column="5" Margin="0,0,5,0">
                                    <TextBlock Text="{x:Bind TotalPrice}" HorizontalAlignment="Center" Foreground="Red" Margin="5" />
                                </Border>
                             
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

              
            </StackPanel>
        </ScrollViewer>

      
      
    </Grid>

</Page>
